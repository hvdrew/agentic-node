{
  "name": "agentic-node",
  "version": "1.0.0",
  "bin": {
    "jarvis": "./index.ts"
  },
  "description": "Agentic LLM setup using local models",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "docker compose up -d && docker compose exec app bun run index.ts",
    "dev:rebuild": "docker compose up -d --build && docker compose exec app bun run index.ts",
    "dev:full-rebuild": "docker compose down -v && npm run dev:rebuild",
    "dev:stop": "docker compose down"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^25.6.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-sdk-tool/parser": "^4.1.20",
    "@ai-sdk/openai": "^3.0.53",
    "ai": "^6.0.161",
    "zod": "^4.3.6"
  },
  "private": true
}
